# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.coverage
.tox/

# Virtualenvs
.venv/
venv/
env/

# IDE
.idea/
.vscode/

# Notebook outputs (keep notebooks themselves)
.ipynb_checkpoints/

# Local data / outputs (everything lives on Drive when running on Colab)
data/raw/
data/processed/
# downloaded Flickr CSVs (fetched fresh by colab_flickr.ipynb)
data/flickr/
checkpoints/
results/*
# ...but DO track the small Flickr result/literature artifacts (deliverables).
!results/flickr/

# Downloaded papers — kept locally only (copyright + repo size).
# The bibliography (articles/README.md) and downloader (articles/_download.sh) ARE tracked.
articles/*.pdf

# OS
.DS_Store
Thumbs.db
