# Pinned versions matching implementation_guide.md / roadmap.md.
# These are the versions Colab T4 runtimes are compatible with as of the spec.
# torch and torch_geometric must be co-installed; PyG 2.5+ does NOT need
# separate torch-scatter / torch-sparse wheels on Colab.

torch==2.3.0
torch_geometric==2.5.3
numpy==1.26.4
pandas==2.1.3
scikit-learn==1.4.2
tqdm==4.66.4
pyarrow==15.0.2
tensorboardX==2.6.2.2

# Test-only (not needed at training time)
pytest==8.1.1
